apiVersion: v1
kind: Service
metadata:
  name: bugged-service
  namespace: "{{ meta.namespace }}"
spec:
  type: NodePort
  selector:
    test: test
  ports:
    - protocol: TCP
      nodePort: {{custom_resource_value}} # THIS WORKS
      port: 80 # pod/svc internal port maps to...
      targetPort: 80 # container internal port
